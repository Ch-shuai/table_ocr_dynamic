# Stock Table OCR Web 应用实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 stock-table-ocr-dynamic 项目构建一个 Flask Web 界面，支持上传截图、配置参数、执行 OCR 解析，并以 Tab 形式展示所有输出内容。

**Architecture:** 单文件 Flask 应用（`web/app.py`），服务端渲染 Jinja2 模板。上传图片保存到临时目录，调用现有 `parse_image` 函数执行解析，读取所有输出文件注入模板渲染。不保存历史记录。

**Tech Stack:** Flask, Jinja2, 原生 CSS/JS

---

## 文件结构

```
web/
  app.py              # Flask 入口 + 所有路由
  __init__.py
  templates/
    base.html         # 基础布局（HTML 骨架、CSS/JS 引入）
    index.html        # 主页面：上传表单 + 结果 Tab 区
  static/
    style.css         # 所有样式
    app.js            # Tab 切换、折叠面板、图片预览
uploads/              # 上传图片临时目录（加入 .gitignore）
```

---

### Task 1: 项目基础配置

**Files:**
- Modify: `.gitignore`
- Modify: `pyproject.toml`
- Create: `web/__init__.py`

**Context:** 添加 Flask 依赖，创建 web 包目录，忽略上传文件目录。

- [ ] **Step 1: 修改 .gitignore 添加 uploads/**

  ```bash
  echo "uploads/" >> .gitignore
  ```

- [ ] **Step 2: 修改 pyproject.toml 添加 Flask 依赖**

  在 `[project]` 的 `dependencies` 列表中添加 `"flask>=3.0"`：

  ```toml
  dependencies = [
    "flask>=3.0",
    "opencv-python>=4.8",
    # ... existing deps
  ]
  ```

- [ ] **Step 3: 安装 Flask**

  ```bash
  source .venv/bin/activate && pip install flask
  ```

- [ ] **Step 4: 创建 web/__init__.py**

  ```python
  # web/__init__.py
  ```
  空文件即可，标记为 Python 包。

- [ ] **Step 5: Commit**

  ```bash
  git add .gitignore pyproject.toml web/__init__.py
  git commit -m "chore: add Flask dependency and web package structure"
  ```

---

### Task 2: Flask 应用骨架 + 基础模板

**Files:**
- Create: `web/app.py`
- Create: `web/templates/base.html`

- [ ] **Step 1: 创建 web/templates/base.html**

  ```html
  <!DOCTYPE html>
  <html lang="zh-CN">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Stock Table OCR{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Stock Table OCR</h1>
        <p>股票行情截图结构化识别</p>
      </header>
      <main>
        {% block content %}{% endblock %}
      </main>
    </div>
    <script src="{{ url_for('static', filename='app.js') }}"></script>
  </body>
  </html>
  ```

- [ ] **Step 2: 创建 web/app.py 骨架**

  ```python
  from __future__ import annotations

  import os
  from datetime import datetime
  from pathlib import Path

  from flask import Flask, render_template, request, redirect, url_for, send_from_directory

  app = Flask(__name__, template_folder="templates", static_folder="static")
  app.config["MAX_CONTENT_LENGTH"] = 16 * 1024 * 1024  # 16MB

  BASE_DIR = Path(__file__).parent.parent
  UPLOAD_DIR = BASE_DIR / "uploads"
  UPLOAD_DIR.mkdir(exist_ok=True)


  @app.route("/")
  def index():
      return render_template("index.html")


  if __name__ == "__main__":
      app.run(debug=True, port=5000)
  ```

- [ ] **Step 3: 验证 Flask 能启动**

  ```bash
  source .venv/bin/activate
  cd web && python app.py
  # 浏览器访问 http://127.0.0.1:5000/ 应看到空白页面（模板还没内容）
  # Ctrl+C 停止
  ```

- [ ] **Step 4: Commit**

  ```bash
  git add web/
  git commit -m "feat: Flask app skeleton and base template"
  ```

---

### Task 3: 上传表单 + 参数配置页面

**Files:**
- Create: `web/templates/index.html`（上传表单部分）
- Create: `web/static/style.css`

- [ ] **Step 1: 创建 index.html 上传表单**

  ```html
  {% extends "base.html" %}

  {% block content %}
  <div class="upload-section">
    <form action="{{ url_for('parse') }}" method="post" enctype="multipart/form-data">
      <div class="drop-zone" id="dropZone">
        <input type="file" name="image" accept="image/png,image/jpeg,image/jpg" id="fileInput" required>
        <p>📁 拖拽图片到这里 或 <span class="clickable">点击选择</span></p>
        <p class="hint">支持 PNG、JPG 格式</p>
        <div class="file-name" id="fileName"></div>
      </div>

      <details class="config-panel" open>
        <summary>⚙️ 参数配置</summary>
        <div class="config-grid">
          <label>
            快照时间
            <input type="text" name="snapshot_time" placeholder="2026-05-29 10:30:00">
          </label>
          <label>
            最低置信度
            <input type="range" name="min_confidence" min="0.5" max="1.0" step="0.01" value="0.85" id="confSlider">
            <span id="confValue">0.85</span>
          </label>
          <label class="checkbox">
            <input type="checkbox" name="binarize_cells"> 单元格二值化
          </label>
          <label class="checkbox">
            <input type="checkbox" name="detect_only"> 仅检测表格结构（不跑 OCR）
          </label>
          <label class="checkbox">
            <input type="checkbox" name="debug_cells" checked> 保存单元格裁剪图
          </label>
        </div>
      </details>

      <button type="submit" class="submit-btn">🚀 运行解析</button>
    </form>
  </div>
  {% endblock %}
  ```

- [ ] **Step 2: 创建基础样式 style.css**

  ```css
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    background: #f5f5f5; color: #333; line-height: 1.6;
  }
  .container { max-width: 1400px; margin: 0 auto; padding: 20px; }
  header { text-align: center; margin-bottom: 30px; }
  header h1 { color: #1a73e8; font-size: 28px; }
  header p { color: #666; }

  /* Upload */
  .upload-section { background: #fff; border-radius: 8px; padding: 30px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
  .drop-zone {
    border: 2px dashed #ccc; border-radius: 8px; padding: 40px; text-align: center;
    transition: border-color 0.2s; cursor: pointer; position: relative;
  }
  .drop-zone:hover, .drop-zone.dragover { border-color: #1a73e8; background: #f8faff; }
  .drop-zone input[type="file"] { position: absolute; inset: 0; opacity: 0; cursor: pointer; }
  .drop-zone .clickable { color: #1a73e8; text-decoration: underline; }
  .drop-zone .hint { font-size: 12px; color: #999; margin-top: 8px; }
  .drop-zone .file-name { margin-top: 10px; font-weight: 500; color: #1a73e8; }

  /* Config */
  .config-panel { margin-top: 20px; padding: 15px; background: #f8f9fa; border-radius: 6px; }
  .config-panel summary { cursor: pointer; font-weight: 500; user-select: none; }
  .config-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 15px; margin-top: 15px; }
  .config-grid label { display: flex; flex-direction: column; gap: 4px; font-size: 14px; }
  .config-grid input[type="text"] { padding: 8px 12px; border: 1px solid #ddd; border-radius: 4px; }
  .config-grid input[type="range"] { width: 100%; }
  .config-grid .checkbox { flex-direction: row; align-items: center; gap: 8px; }

  /* Submit */
  .submit-btn {
    display: block; width: 100%; margin-top: 20px; padding: 14px;
    background: #1a73e8; color: #fff; border: none; border-radius: 6px;
    font-size: 16px; cursor: pointer; transition: background 0.2s;
  }
  .submit-btn:hover { background: #1557b0; }
  ```

- [ ] **Step 3: 在 app.py 添加 parse 路由（占位）**

  在 `web/app.py` 中添加：

  ```python
  @app.route("/parse", methods=["POST"])
  def parse():
      # Placeholder - will be implemented in Task 4
      return redirect(url_for("index"))
  ```

- [ ] **Step 4: 验证上传页面**

  ```bash
  cd web && python app.py
  # 访问 http://127.0.0.1:5000/ 应看到上传区域 + 参数配置面板
  ```

- [ ] **Step 5: Commit**

  ```bash
  git add web/templates/index.html web/static/style.css web/app.py
  git commit -m "feat: upload form and config panel UI"
  ```

---

### Task 4: /parse 路由 — 文件保存 + 调用解析

**Files:**
- Modify: `web/app.py`（添加 parse 路由逻辑）

- [ ] **Step 1: 修改 app.py 添加完整 parse 路由**

  替换占位 parse 路由：

  ```python
  import sys
  sys.path.insert(0, str(BASE_DIR / "src"))

  from stock_table_ocr.parser import compute_runtime_layout, parse_image
  from stock_table_ocr.config import load_schema
  from stock_table_ocr.ocr_engine import build_ocr_engine
  from stock_table_ocr.image_utils import read_image
  from stock_table_ocr.outputs import (
      write_csv, write_excel, write_jsonl, write_stock_json_files,
      write_error_report, write_correction_log, write_runtime_layout,
  )


  @app.route("/parse", methods=["POST"])
  def parse():
      file = request.files.get("image")
      if not file or file.filename == "":
          return "未选择图片", 400

      run_id = datetime.now().strftime("run_%Y%m%d_%H%M%S")
      run_upload_dir = UPLOAD_DIR / run_id
      run_upload_dir.mkdir(parents=True, exist_ok=True)
      image_path = run_upload_dir / "input.png"
      file.save(image_path)

      schema_path = BASE_DIR / "configs" / "stock_table_schema.json"
      stock_dict_path = BASE_DIR / "data" / "stock_code_name_sample.csv"
      output_dir = BASE_DIR / "output" / run_id
      output_dir.mkdir(parents=True, exist_ok=True)

      snapshot_time = request.form.get("snapshot_time") or None
      min_confidence = float(request.form.get("min_confidence", "0.85"))
      binarize_cells = bool(request.form.get("binarize_cells"))
      detect_only = bool(request.form.get("detect_only"))
      debug_cells = bool(request.form.get("debug_cells"))

      if detect_only:
          image = read_image(str(image_path))
          schema = load_schema(str(schema_path))
          columns, rows, warnings = compute_runtime_layout(image, schema, ocr_engine=None)
          return render_template("index.html",
              has_result=True,
              detect_only=True,
              run_id=run_id,
              columns=columns,
              rows=rows,
              warnings=warnings,
          )

      ocr_engine = build_ocr_engine("paddle", lang="ch")
      debug_dir = output_dir / "debug_cells" if debug_cells else None

      result = parse_image(
          image_path=str(image_path),
          schema_path=str(schema_path),
          ocr_engine=ocr_engine,
          stock_dict_path=str(stock_dict_path),
          snapshot_time=snapshot_time,
          output_debug_cells_dir=debug_dir,
          min_confidence=min_confidence,
          binarize_cells=binarize_cells,
      )

      # Save outputs
      from stock_table_ocr.config import column_keys
      field_order = column_keys(load_schema(str(schema_path)))
      stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
      write_csv(result.records, output_dir / f"market_table_{stamp}.csv", field_order)
      write_excel(result.records, output_dir / f"market_table_{stamp}.xlsx", field_order)
      write_jsonl(result.records, output_dir / f"market_table_{stamp}.jsonl")
      write_stock_json_files(result.records, output_dir / "stocks")
      write_error_report(result.records, output_dir / "review" / f"ocr_error_cells_{stamp}.csv")
      write_correction_log(result.records, output_dir / "review" / f"correction_log_{stamp}.md")
      write_runtime_layout(result, output_dir / f"runtime_layout_{stamp}.json")

      return redirect(url_for("result", run_id=run_id))
  ```

- [ ] **Step 2: 添加 result 路由占位**

  ```python
  @app.route("/result/<run_id>")
  def result(run_id):
      # Placeholder - full implementation in Task 5
      return render_template("index.html", has_result=True, run_id=run_id)
  ```

- [ ] **Step 3: 测试 detect-only 模式**

  ```bash
  cd web && python app.py
  # 上传 samples/white_market_table_sample.png
  # 勾选"仅检测表格结构"
  # 提交后应看到页面刷新，带 has_result=True
  ```

- [ ] **Step 4: Commit**

  ```bash
  git add web/app.py
  git commit -m "feat: implement /parse route with detect-only and full OCR"
  ```

---

### Task 5: 结果展示页面 — Tab 框架 + 数据加载

**Files:**
- Modify: `web/app.py`（result 路由加载数据）
- Modify: `web/templates/index.html`（添加结果 Tab 框架）
- Modify: `web/static/style.css`（Tab 样式）
- Modify: `web/static/app.js`（Tab 切换）

- [ ] **Step 1: 修改 result 路由加载所有输出文件**

  ```python
  import json
  import csv
  from dataclasses import asdict

  @app.route("/result/<run_id>")
  def result(run_id):
      output_dir = BASE_DIR / "output" / run_id
      if not output_dir.exists():
          return "结果不存在", 404

      # Find output files by pattern
      csv_files = list(output_dir.glob("market_table_*.csv"))
      jsonl_files = list(output_dir.glob("market_table_*.jsonl"))
      layout_files = list(output_dir.glob("runtime_layout_*.json"))
      error_files = list(output_dir.glob("review/ocr_error_cells_*.csv"))
      log_files = list(output_dir.glob("review/correction_log_*.md"))

      # Load CSV data
      csv_data = []
      csv_headers = []
      if csv_files:
          with open(csv_files[0], "r", encoding="utf-8-sig") as f:
              reader = csv.DictReader(f)
              csv_headers = reader.fieldnames or []
              csv_data = list(reader)

      # Load JSONL data
      jsonl_data = []
      if jsonl_files:
          with open(jsonl_files[0], "r", encoding="utf-8") as f:
              jsonl_data = [json.loads(line) for line in f if line.strip()]

      # Load layout data
      layout_data = {}
      if layout_files:
          with open(layout_files[0], "r", encoding="utf-8") as f:
              layout_data = json.load(f)

      # Load error report
      error_data = []
      if error_files:
          with open(error_files[0], "r", encoding="utf-8-sig") as f:
              error_data = list(csv.DictReader(f))

      # Load correction log
      log_content = ""
      if log_files:
          with open(log_files[0], "r", encoding="utf-8") as f:
              log_content = f.read()

      # Load stock JSON files
      stock_files = []
      stocks_dir = output_dir / "stocks"
      if stocks_dir.exists():
          stock_files = sorted([p.name for p in stocks_dir.glob("*.json")])

      # Load debug cell images
      debug_cells = []
      debug_dir = output_dir / "debug_cells"
      if debug_dir.exists():
          subdirs = sorted([d for d in debug_dir.iterdir() if d.is_dir()])
          for subdir in subdirs:
              images = sorted([p.name for p in subdir.glob("*.png")])
              if images:
                  debug_cells.append({"subdir": subdir.name, "images": images})

      return render_template("index.html",
          has_result=True,
          run_id=run_id,
          csv_headers=csv_headers,
          csv_data=csv_data,
          jsonl_data=jsonl_data,
          layout_data=layout_data,
          error_data=error_data,
          log_content=log_content,
          stock_files=stock_files,
          debug_cells=debug_cells,
      )
  ```

- [ ] **Step 2: 添加静态文件路由**

  ```python
  @app.route("/cell_image/<run_id>/<subdir>/<filename>")
  def cell_image(run_id, subdir, filename):
      return send_from_directory(
          BASE_DIR / "output" / run_id / "debug_cells" / subdir,
          filename,
      )

  @app.route("/original_image/<run_id>")
  def original_image(run_id):
      return send_from_directory(UPLOAD_DIR / run_id, "input.png")
  ```

- [ ] **Step 3: 修改 index.html 添加结果 Tab 框架**

  在 `{% block content %}` 末尾、form 之后添加：

  ```html
  {% if has_result %}
  <div class="result-section">
    <div class="tabs">
      <button class="tab-btn active" data-tab="preview">原始预览</button>
      <button class="tab-btn" data-tab="table">数据表格</button>
      <button class="tab-btn" data-tab="jsonl">JSONL</button>
      <button class="tab-btn" data-tab="layout">布局坐标</button>
      <button class="tab-btn" data-tab="errors">错误报告</button>
      <button class="tab-btn" data-tab="cells">单元格图片</button>
      <button class="tab-btn" data-tab="stocks">单股 JSON</button>
    </div>

    <div class="tab-content">
      <div class="tab-panel active" id="tab-preview">
        {% include "tabs/preview.html" %}
      </div>
      <div class="tab-panel" id="tab-table">
        {% include "tabs/table.html" %}
      </div>
      <div class="tab-panel" id="tab-jsonl">
        {% include "tabs/jsonl.html" %}
      </div>
      <div class="tab-panel" id="tab-layout">
        {% include "tabs/layout.html" %}
      </div>
      <div class="tab-panel" id="tab-errors">
        {% include "tabs/errors.html" %}
      </div>
      <div class="tab-panel" id="tab-cells">
        {% include "tabs/cells.html" %}
      </div>
      <div class="tab-panel" id="tab-stocks">
        {% include "tabs/stocks.html" %}
      </div>
    </div>
  </div>
  {% endif %}
  ```

- [ ] **Step 4: 创建 tabs 目录和占位模板**

  ```bash
  mkdir -p web/templates/tabs
  ```

  每个文件先写占位内容：

  `web/templates/tabs/preview.html`:
  ```html
  <div class="preview-container">
    <p>原始图片预览（待实现）</p>
  </div>
  ```

  `web/templates/tabs/table.html`:
  ```html
  <div class="table-container">
    <p>数据表格（待实现）</p>
  </div>
  ```

  `web/templates/tabs/jsonl.html`:
  ```html
  <div class="jsonl-container">
    <p>JSONL（待实现）</p>
  </div>
  ```

  `web/templates/tabs/layout.html`:
  ```html
  <div class="layout-container">
    <p>布局坐标（待实现）</p>
  </div>
  ```

  `web/templates/tabs/errors.html`:
  ```html
  <div class="errors-container">
    <p>错误报告（待实现）</p>
  </div>
  ```

  `web/templates/tabs/cells.html`:
  ```html
  <div class="cells-container">
    <p>单元格图片（待实现）</p>
  </div>
  ```

  `web/templates/tabs/stocks.html`:
  ```html
  <div class="stocks-container">
    <p>单股 JSON（待实现）</p>
  </div>
  ```

- [ ] **Step 5: 添加 Tab 切换样式**

  在 `style.css` 末尾添加：

  ```css
  /* Results */
  .result-section { margin-top: 30px; background: #fff; border-radius: 8px; padding: 20px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
  .tabs { display: flex; gap: 4px; border-bottom: 2px solid #e0e0e0; margin-bottom: 20px; flex-wrap: wrap; }
  .tab-btn { padding: 10px 16px; border: none; background: none; cursor: pointer; font-size: 14px; color: #666; border-bottom: 2px solid transparent; margin-bottom: -2px; transition: all 0.2s; }
  .tab-btn:hover { color: #1a73e8; }
  .tab-btn.active { color: #1a73e8; border-bottom-color: #1a73e8; font-weight: 500; }
  .tab-panel { display: none; }
  .tab-panel.active { display: block; }
  ```

- [ ] **Step 6: 添加 Tab 切换 JS**

  创建 `web/static/app.js`：

  ```javascript
  document.addEventListener('DOMContentLoaded', function() {
    // Tab switching
    document.querySelectorAll('.tab-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
        document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
        btn.classList.add('active');
        const tabId = 'tab-' + btn.dataset.tab;
        document.getElementById(tabId)?.classList.add('active');
      });
    });

    // File drop zone
    const dropZone = document.getElementById('dropZone');
    const fileInput = document.getElementById('fileInput');
    const fileName = document.getElementById('fileName');
    const confSlider = document.getElementById('confSlider');
    const confValue = document.getElementById('confValue');

    if (dropZone) {
      dropZone.addEventListener('dragover', e => { e.preventDefault(); dropZone.classList.add('dragover'); });
      dropZone.addEventListener('dragleave', () => dropZone.classList.remove('dragover'));
      dropZone.addEventListener('drop', e => {
        e.preventDefault(); dropZone.classList.remove('dragover');
        if (e.dataTransfer.files.length) {
          fileInput.files = e.dataTransfer.files;
          fileName.textContent = e.dataTransfer.files[0].name;
        }
      });
      fileInput.addEventListener('change', () => {
        if (fileInput.files.length) fileName.textContent = fileInput.files[0].name;
      });
    }

    if (confSlider && confValue) {
      confSlider.addEventListener('input', () => confValue.textContent = confSlider.value);
    }
  });
  ```

- [ ] **Step 7: 验证 Tab 切换**

  ```bash
  cd web && python app.py
  # 上传图片 → 仅检测表格结构 → 结果页应显示 7 个 Tab，点击可切换
  ```

- [ ] **Step 8: Commit**

  ```bash
  git add web/
  git commit -m "feat: result tab framework with data loading"
  ```

---

### Task 6: 原始预览 Tab（图片 + 行列线叠加）

**Files:**
- Modify: `web/templates/tabs/preview.html`
- Modify: `web/static/style.css`

- [ ] **Step 1: 修改 preview.html**

  ```html
  <div class="preview-container">
    <div class="image-wrapper" id="imageWrapper">
      <img src="{{ url_for('original_image', run_id=run_id) }}" alt="原始图片" id="originalImage">
      {% if layout_data.columns %}
        {% for col in layout_data.columns %}
        <div class="grid-line v-line" style="left:{{ (col.x1 / 2047 * 100) }}%;top:0;height:100%;"></div>
        <div class="grid-line v-line" style="left:{{ (col.x2 / 2047 * 100) }}%;top:0;height:100%;"></div>
        {% endfor %}
      {% endif %}
      {% if layout_data.rows %}
        {% for row in layout_data.rows %}
        <div class="grid-line h-line" style="top:{{ (row.y1 / 1015 * 100) }}%;left:0;width:100%;"></div>
        <div class="grid-line h-line" style="top:{{ (row.y2 / 1015 * 100) }}%;left:0;width:100%;"></div>
        {% endfor %}
      {% endif %}
    </div>
    <div class="preview-info">
      <p>列数: {{ layout_data.columns | length }} | 行数: {{ layout_data.rows | length }}</p>
      {% if layout_data.warnings %}
        <div class="warnings">
          {% for w in layout_data.warnings %}
            <p class="warning">⚠️ {{ w }}</p>
          {% endfor %}
        </div>
      {% endif %}
    </div>
  </div>
  ```

  **注意**：这里使用图片原始尺寸 2047x1015 计算百分比。需要改为动态获取图片尺寸。用 JS 在图片加载后重算。

- [ ] **Step 2: 修改 preview.html 为 JS 动态叠加线**

  更可靠的方案：图片加载完成后通过 JS 读取实际尺寸，然后设置线的位置。

  ```html
  <div class="preview-container">
    <div class="image-wrapper" id="imageWrapper">
      <img src="{{ url_for('original_image', run_id=run_id) }}" alt="原始图片" id="originalImage"
           data-columns='{{ layout_data.columns | tojson }}'
           data-rows='{{ layout_data.rows | tojson }}'>
    </div>
    <div class="preview-info">
      <p>列数: <strong>{{ layout_data.columns | length }}</strong> | 行数: <strong>{{ layout_data.rows | length }}</strong></p>
      {% if layout_data.warnings %}
        <div class="warnings">
          {% for w in layout_data.warnings %}<p class="warning">⚠️ {{ w }}</p>{% endfor %}
        </div>
      {% endif %}
    </div>
  </div>
  ```

- [ ] **Step 3: 在 app.js 添加图片叠加线逻辑**

  ```javascript
  // Image grid overlay
  const img = document.getElementById('originalImage');
  const wrapper = document.getElementById('imageWrapper');
  if (img && wrapper) {
    img.addEventListener('load', () => {
      const w = img.naturalWidth, h = img.naturalHeight;
      const cols = JSON.parse(img.dataset.columns || '[]');
      const rows = JSON.parse(img.dataset.rows || '[]');
      cols.forEach(col => {
        createLine(wrapper, col.x1 / w * 100, 0, 'v');
        createLine(wrapper, col.x2 / w * 100, 0, 'v');
      });
      rows.forEach(row => {
        createLine(wrapper, 0, row.y1 / h * 100, 'h');
        createLine(wrapper, 0, row.y2 / h * 100, 'h');
      });
    });
  }
  function createLine(wrapper, x, y, type) {
    const line = document.createElement('div');
    line.className = 'grid-line ' + (type === 'v' ? 'v-line' : 'h-line');
    if (type === 'v') { line.style.left = x + '%'; line.style.top = '0'; line.style.height = '100%'; }
    else { line.style.top = y + '%'; line.style.left = '0'; line.style.width = '100%'; }
    wrapper.appendChild(line);
  }
  ```

- [ ] **Step 4: 添加叠加线样式**

  ```css
  .image-wrapper { position: relative; display: inline-block; max-width: 100%; overflow: auto; }
  .image-wrapper img { max-width: 100%; display: block; border: 1px solid #ddd; }
  .grid-line { position: absolute; pointer-events: none; }
  .v-line { width: 1px; background: rgba(255, 0, 0, 0.5); }
  .h-line { height: 1px; background: rgba(255, 0, 0, 0.5); }
  .preview-info { margin-top: 15px; padding: 10px; background: #f8f9fa; border-radius: 4px; }
  .warnings { margin-top: 10px; }
  .warning { color: #e65100; font-size: 13px; }
  ```

- [ ] **Step 5: 验证**

  上传图片 → 运行解析 → 切换到"原始预览"Tab，应看到图片上有红色行列线。

- [ ] **Step 6: Commit**

  ```bash
  git add web/
  git commit -m "feat: original image preview with grid overlay"
  ```

---

### Task 7: 数据表格 Tab

**Files:**
- Modify: `web/templates/tabs/table.html`
- Modify: `web/static/style.css`

- [ ] **Step 1: 修改 table.html**

  ```html
  <div class="table-container">
    <div class="table-scroll">
      <table class="data-table">
        <thead>
          <tr>
            {% for h in csv_headers %}<th>{{ h }}</th>{% endfor %}
          </tr>
        </thead>
        <tbody>
          {% for row in csv_data %}
          <tr>
            {% for h in csv_headers %}<td>{{ row[h] }}</td>{% endfor %}
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
    <p class="table-meta">共 {{ csv_data | length }} 行数据</p>
  </div>
  ```

- [ ] **Step 2: 添加表格样式**

  ```css
  .table-scroll { overflow-x: auto; max-height: 600px; overflow-y: auto; }
  .data-table { width: 100%; border-collapse: collapse; font-size: 13px; }
  .data-table th { position: sticky; top: 0; background: #1a73e8; color: #fff; padding: 10px 12px; text-align: left; font-weight: 500; white-space: nowrap; }
  .data-table td { padding: 8px 12px; border-bottom: 1px solid #e0e0e0; white-space: nowrap; }
  .data-table tbody tr:hover { background: #f5f5f5; }
  .data-table tbody tr:nth-child(even) { background: #fafafa; }
  .table-meta { margin-top: 10px; color: #666; font-size: 13px; }
  ```

- [ ] **Step 3: 验证**

  上传图片 → 运行完整 OCR → "数据表格"Tab 应显示 32 行 × 24 列的表格。

- [ ] **Step 4: Commit**

  ```bash
  git add web/
  git commit -m "feat: data table tab with CSV rendering"
  ```

---

### Task 8: JSONL Tab

**Files:**
- Modify: `web/templates/tabs/jsonl.html`
- Modify: `web/static/style.css`

- [ ] **Step 1: 修改 jsonl.html**

  ```html
  <div class="jsonl-container">
    {% for record in jsonl_data %}
    <details class="jsonl-record">
      <summary>
        <span class="record-code">{{ record.fields.code or 'N/A' }}</span>
        <span class="record-name">{{ record.fields.name or '' }}</span>
        {% if record.validation_errors %}
          <span class="error-badge">{{ record.validation_errors | length }} 个错误</span>
        {% endif %}
      </summary>
      <pre class="json-code">{{ record | tojson_pretty }}</pre>
    </details>
    {% endfor %}
  </div>
  ```

  **注意**：Jinja2 没有内置 `tojson_pretty`，需要在 Flask 中注册自定义 filter。

- [ ] **Step 2: 在 app.py 注册 tojson_pretty filter**

  ```python
  import json

  @app.template_filter('tojson_pretty')
  def tojson_pretty(value):
      return json.dumps(value, ensure_ascii=False, indent=2)
  ```

- [ ] **Step 3: 添加 JSONL 样式**

  ```css
  .jsonl-container { max-height: 600px; overflow-y: auto; }
  .jsonl-record { margin-bottom: 8px; border: 1px solid #e0e0e0; border-radius: 4px; }
  .jsonl-record summary { padding: 10px 15px; cursor: pointer; background: #f8f9fa; font-size: 14px; display: flex; gap: 12px; align-items: center; }
  .jsonl-record summary:hover { background: #f0f0f0; }
  .record-code { font-family: monospace; font-weight: 600; color: #1a73e8; }
  .record-name { color: #333; }
  .error-badge { background: #ff5722; color: #fff; font-size: 11px; padding: 2px 8px; border-radius: 10px; margin-left: auto; }
  .json-code { padding: 15px; background: #263238; color: #aed581; font-size: 12px; line-height: 1.5; overflow-x: auto; margin: 0; }
  ```

- [ ] **Step 4: 验证**

  JSONL Tab 应显示 32 个可折叠的记录，包含股票代码、名称和 JSON 内容。

- [ ] **Step 5: Commit**

  ```bash
  git add web/
  git commit -m "feat: JSONL tab with foldable records"
  ```

---

### Task 9: 布局坐标 Tab

**Files:**
- Modify: `web/templates/tabs/layout.html`
- Modify: `web/static/style.css`

- [ ] **Step 1: 修改 layout.html**

  ```html
  <div class="layout-container">
    <div class="layout-grid">
      <div class="layout-section">
        <h3>列坐标 ({{ layout_data.columns | length }} 列)</h3>
        <div class="table-scroll">
          <table class="data-table">
            <thead><tr><th>字段</th><th>key</th><th>x1</th><th>x2</th><th>宽度</th></tr></thead>
            <tbody>
              {% for col in layout_data.columns %}
              <tr>
                <td>{{ col.title }}</td>
                <td><code>{{ col.key }}</code></td>
                <td>{{ col.x1 }}</td>
                <td>{{ col.x2 }}</td>
                <td>{{ col.x2 - col.x1 }}</td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
      <div class="layout-section">
        <h3>行坐标 ({{ layout_data.rows | length }} 行)</h3>
        <div class="table-scroll" style="max-height: 500px;">
          <table class="data-table">
            <thead><tr><th>索引</th><th>y1</th><th>y2</th><th>高度</th><th>锚点文本</th></tr></thead>
            <tbody>
              {% for row in layout_data.rows %}
              <tr>
                <td>{{ row.index }}</td>
                <td>{{ row.y1 }}</td>
                <td>{{ row.y2 }}</td>
                <td>{{ row.y2 - row.y1 }}</td>
                <td>{{ row.anchor_text or '-' }}</td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  ```

- [ ] **Step 2: 添加布局样式**

  ```css
  .layout-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
  .layout-section h3 { margin-bottom: 12px; font-size: 16px; color: #333; }
  @media (max-width: 900px) { .layout-grid { grid-template-columns: 1fr; } }
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add web/
  git commit -m "feat: layout coordinates tab"
  ```

---

### Task 10: 错误报告 Tab

**Files:**
- Modify: `web/templates/tabs/errors.html`
- Modify: `web/static/style.css`

- [ ] **Step 1: 修改 errors.html**

  ```html
  <div class="errors-container">
    <div class="errors-grid">
      <div class="error-section">
        <h3>低置信度 / 格式异常 ({{ error_data | length }} 条)</h3>
        {% if error_data %}
        <div class="table-scroll" style="max-height: 400px;">
          <table class="data-table">
            <thead><tr><th>行</th><th>字段</th><th>原始值</th><th>标准化</th><th>置信度</th><th>原因</th></tr></thead>
            <tbody>
              {% for err in error_data %}
              {% if err.level != 'info' %}
              <tr>
                <td>{{ err.row_index or '-' }}</td>
                <td>{{ err.field or '-' }}</td>
                <td>{{ err.raw_text or '-' }}</td>
                <td>{{ err.normalized or '-' }}</td>
                <td>{{ err.confidence or '-' }}</td>
                <td><code>{{ err.reason }}</code></td>
              </tr>
              {% endif %}
              {% endfor %}
            </tbody>
          </table>
        </div>
        {% else %}
        <p class="no-errors">✅ 无异常记录</p>
        {% endif %}
      </div>

      <div class="error-section">
        <h3>修正日志</h3>
        {% if log_content %}
        <pre class="log-content">{{ log_content }}</pre>
        {% else %}
        <p class="no-errors">无修正记录</p>
        {% endif %}
      </div>
    </div>
  </div>
  ```

- [ ] **Step 2: 添加错误样式**

  ```css
  .errors-grid { display: grid; grid-template-columns: 1fr; gap: 20px; }
  .error-section h3 { margin-bottom: 12px; font-size: 16px; }
  .no-errors { color: #2e7d32; padding: 20px; background: #e8f5e9; border-radius: 4px; }
  .log-content { padding: 15px; background: #f5f5f5; border-radius: 4px; font-size: 13px; line-height: 1.6; overflow-x: auto; white-space: pre-wrap; max-height: 400px; overflow-y: auto; }
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add web/
  git commit -m "feat: error report tab"
  ```

---

### Task 11: 单元格图片 Tab

**Files:**
- Modify: `web/templates/tabs/cells.html`
- Modify: `web/static/style.css`

- [ ] **Step 1: 修改 cells.html**

  ```html
  <div class="cells-container">
    {% for group in debug_cells %}
    <details class="cell-group" {% if loop.first %}open{% endif %}>
      <summary>{{ group.subdir }} ({{ group.images | length }} 张)</summary>
      <div class="cell-grid">
        {% for img in group.images %}
        <div class="cell-thumb">
          <img src="{{ url_for('cell_image', run_id=run_id, subdir=group.subdir, filename=img) }}" alt="{{ img }}">
          <span class="cell-label">{{ img }}</span>
        </div>
        {% endfor %}
      </div>
    </details>
    {% endfor %}
  </div>
  ```

- [ ] **Step 2: 添加单元格图片样式**

  ```css
  .cell-group { margin-bottom: 12px; border: 1px solid #e0e0e0; border-radius: 4px; }
  .cell-group summary { padding: 10px 15px; cursor: pointer; background: #f8f9fa; font-size: 14px; }
  .cell-group summary:hover { background: #f0f0f0; }
  .cell-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 10px; padding: 15px; }
  .cell-thumb { text-align: center; }
  .cell-thumb img { max-width: 100%; border: 1px solid #ddd; border-radius: 4px; }
  .cell-label { display: block; font-size: 10px; color: #666; margin-top: 4px; word-break: break-all; }
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add web/
  git commit -m "feat: debug cell images tab with row grouping"
  ```

---

### Task 12: 单股 JSON Tab

**Files:**
- Modify: `web/templates/tabs/stocks.html`
- Modify: `web/app.py`（添加 stock_json 路由）

- [ ] **Step 1: 添加 stock JSON 文件路由**

  ```python
  @app.route("/stock_json/<run_id>/<filename>")
  def stock_json(run_id, filename):
      return send_from_directory(BASE_DIR / "output" / run_id / "stocks", filename)
  ```

- [ ] **Step 2: 修改 stocks.html**

  ```html
  <div class="stocks-container">
    {% for fname in stock_files %}
    <details class="stock-record">
      <summary>{{ fname }}</summary>
      <pre class="json-code" id="stock-{{ loop.index }}">加载中...</pre>
    </details>
    {% endfor %}
  </div>
  ```

- [ ] **Step 3: 在 app.js 添加懒加载**

  ```javascript
  // Lazy load stock JSON content
  document.querySelectorAll('.stock-record').forEach((details, i) => {
    details.addEventListener('toggle', () => {
      if (details.open) {
        const pre = details.querySelector('pre');
        if (pre.textContent === '加载中...') {
          const runId = document.querySelector('.result-section')?.dataset.runId;
          const filename = details.querySelector('summary').textContent;
          fetch(`/stock_json/${runId}/${filename}`)
            .then(r => r.json())
            .then(data => { pre.textContent = JSON.stringify(data, null, 2); })
            .catch(() => { pre.textContent = '加载失败'; });
        }
      }
    });
  });
  ```

  需要在 result-section 上添加 `data-run-id="{{ run_id }}"`。

- [ ] **Step 4: 添加单股样式**

  ```css
  .stock-record { margin-bottom: 8px; border: 1px solid #e0e0e0; border-radius: 4px; }
  .stock-record summary { padding: 10px 15px; cursor: pointer; background: #f8f9fa; font-size: 14px; font-family: monospace; }
  .stock-record summary:hover { background: #f0f0f0; }
  ```

- [ ] **Step 5: Commit**

  ```bash
  git add web/
  git commit -m "feat: individual stock JSON tab with lazy loading"
  ```

---

### Task 13: 样式完善 + 响应式

**Files:**
- Modify: `web/static/style.css`

- [ ] **Step 1: 添加响应式和细节样式**

  ```css
  /* Responsive */
  @media (max-width: 768px) {
    .container { padding: 10px; }
    .tabs { gap: 2px; }
    .tab-btn { padding: 8px 10px; font-size: 12px; }
    .config-grid { grid-template-columns: 1fr; }
    .layout-grid { grid-template-columns: 1fr; }
  }

  /* Loading state */
  .submit-btn:disabled { background: #ccc; cursor: not-allowed; }
  .loading { text-align: center; padding: 40px; color: #666; }
  .loading::after { content: ""; display: inline-block; width: 20px; height: 20px; border: 2px solid #1a73e8; border-top-color: transparent; border-radius: 50%; animation: spin 1s linear infinite; margin-left: 10px; vertical-align: middle; }
  @keyframes spin { to { transform: rotate(360deg); } }

  /* Code blocks */
  code { background: #f0f0f0; padding: 2px 6px; border-radius: 3px; font-size: 12px; font-family: "SF Mono", Monaco, monospace; }
  ```

- [ ] **Step 2: 添加表单提交加载状态**

  在 `app.js` 中：

  ```javascript
  const form = document.querySelector('form');
  if (form) {
    form.addEventListener('submit', () => {
      const btn = form.querySelector('.submit-btn');
      btn.disabled = true;
      btn.textContent = '⏳ 解析中，请稍候...';
    });
  }
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add web/
  git commit -m "style: responsive design and loading states"
  ```

---

### Task 14: 端到端验证

**Files:**
- None（验证步骤）

- [ ] **Step 1: 启动应用**

  ```bash
  cd web && python app.py
  ```

- [ ] **Step 2: 完整测试流程**

  1. 访问 `http://127.0.0.1:5000/`
  2. 上传 `samples/white_market_table_sample.png`
  3. 设置快照时间为 `2026-05-29 10:30:00`
  4. 点击"运行解析"
  5. 等待 PaddleOCR 模型加载（约 10-20 秒）
  6. 验证每个 Tab：
     - **原始预览**：图片上有红色行列线
     - **数据表格**：32 行数据
     - **JSONL**：32 个可折叠记录
     - **布局坐标**：21 列 + 32 行坐标
     - **错误报告**：低置信度字段列表
     - **单元格图片**：按时间戳分组，缩略图网格
     - **单股 JSON**：每只股票的 JSON，点击展开加载

- [ ] **Step 3: 测试 detect-only 模式**

  勾选"仅检测表格结构"→ 运行 → 应只显示"原始预览"和"布局坐标"Tab，无数据表格。

- [ ] **Step 4: Commit**

  ```bash
  git add -A
  git commit -m "feat: complete web app for stock table OCR"
  ```

---

## Spec 覆盖检查

| Spec 要求 | 实现任务 |
|-----------|---------|
| Flask + Jinja2 服务端渲染 | Task 1-2 |
| 上传截图（拖拽/选择） | Task 3 |
| 参数配置表单 | Task 3 |
| 运行 OCR 解析 | Task 4 |
| 原始预览 Tab（图片+行列线） | Task 6 |
| 数据表格 Tab | Task 7 |
| JSONL Tab | Task 8 |
| 布局坐标 Tab | Task 9 |
| 错误报告 Tab | Task 10 |
| 单元格图片 Tab | Task 11 |
| 单股 JSON Tab | Task 12 |
| 不保存历史记录 | 设计于所有任务中（无数据库，纯文件系统） |
| 所有输出在页面显示 | Task 5-12 |

## Placeholder 检查

- ✅ 无 TBD/TODO
- ✅ 所有代码片段完整可直接使用
- ✅ 所有文件路径明确
- ✅ 所有命令和预期输出明确
